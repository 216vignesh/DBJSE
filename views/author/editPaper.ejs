<!-- <!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>EJS Engine</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
        <form method="post" action="/author/papers/update/<%= paper._id %>">
          <h1>Paper Name</h1>
          <div class="form-group">
            <label for="paper_title">Paper</label>
            <input type="text" class="form-control" id="paper_title" name="paper_title" value="<%= paper.paper_title %>">

            <label for="paper_author">Author</label>
            <input type="text" class="form-control" id="paper_author" name="paper_author" value="<%= paper.paper_author %>">
          </div>
          <button type="submit" class="btn btn-default">Update</button>
        </form>
    </div>
  </body>
</html> -->


<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <% include ../../public/partials/head %>
</head>

<body>

  <header class="main-header">
    <% include ../../public/js/header %>
      <div class="showcase container">
        <div class="row">
          <h4 class="col align-center" id="heading">Edit <%= paper.paper_title %> Paper </h4>
        </div>



        <div class="container">
          <form method="post" action="/author/papers/update/<%= paper._id %>">
            <div class="form-group">
              <label style="font-size: larger;" for="paper_title">Paper</label>
              <input type="text" class="form-control" id="paper_title" name="paper_title" value="<%= paper.paper_title %>">

              <label style="font-size: larger;" for="paper_author">Author</label>
              <input type="text" class="form-control" id="paper_author" name="paper_author" value="<%= paper.paper_author %>">
            </div>
            <button type="submit" class="btn btn-default">Update</button>
          </form>
      </div>



  </div>

        <footer>
          <% include ../../public/partials/footer %>
        </footer>

        <script>
          $(document).ready(function() {
            $('.carousel.carousel-slider').carousel({
              fullWidth: true
            });
            $('.button-collapse').sideNav();
            $('.modal').modal();
            $('select').formSelect();
          });

          $(document).ready(function() {
            $('.carousel.carousel-slider').carousel({
              fullWidth: true
            });
            $('.button-collapse').sideNav();
            $('.modal').modal();
          });
          $(".dropdown-trigger").dropdown();
        </script>
</body>

</html>
